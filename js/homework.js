
/**
 * Ejericio 1:
 * Realizar una funcion que dado un array de strings como parametro
 * retorne un nuevo array con todos los strings que tienen como maximo 5 caracteres
 */
    let arrayToCut=["dog", "wolf", "family", "eaten", "camping"]
    //let newItem=[]
    //const maxFive = arr =>arr.forEach(item=>item.length<=5 ? newItem.push(item):'')
    //maxFive(arrayToCut)


    /*  SOLUCION CON FILTER
    const maxFive = arr =>arr.filter(item=>item.length<=5 )
    console.log(maxFive(arrayToCut))*/




/**
 * Ejericio 2:
 * Realizar una funcion que dado un array de nombres de koders como parametro
 * Retorne un nuevo array con todos koders que tienen mas de 1 nombre
 * 
 */
            let kodersNames=["Aaron","Antonio", "Victor Yair", "Luis Carlos", "Victor", "José Omar"]
            //let newItem=[]
            //const twoNames=arr => arr.forEach(item=> item.includes(" ") === true ? newItem.push(item) : '')
            //console.log(twoNames(kodersNames))

            /*  RESOLUCION CON FILTER
            const twoNames=arr => arr.filter(item=> item.includes(" ") === true )
            console.log(twoNames(kodersNames))*/